apiVersion: v1
kind: Service
metadata:
  name: ms-user
  labels:
    app: ms-user
spec:
  selector:
    app: ms-user
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
